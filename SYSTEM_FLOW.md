# SparkPath - Complete System Flow Diagram

## User Flow Visualization

```
┌─────────────────────────────────────────────────────────────────────┐
│                        NEW USER JOURNEY                              │
└─────────────────────────────────────────────────────────────────────┘

1. LANDING & SIGNUP
   ┌──────────┐
   │  Signup  │ → Enter: Email, Password, Name, Demographics
   │   Page   │    (Race, Ethnicity, Location)
   └────┬─────┘
        │
        ↓
2. AI CAREER ASSESSMENT
   ┌──────────────────┐
   │   ChatBot UI     │ ← Socket.io ← AWS Bedrock (Claude)
   │  Assessment Page │
   └────┬─────────────┘
        │ Asks 5-7 questions
        │ One at a time
        │ LLM analyzes responses
        ↓
   ┌──────────────────┐
   │  Category Result │ → Recommended: "MUSIC"
   │                  │   Subcategories: [Producer, Audio Engineer, ...]
   └────┬─────────────┘
        │
        ↓
3. SUBCATEGORY SELECTION
   ┌──────────────────┐
   │  Selection Page  │ → User picks: [Producer, Audio Engineer]
   │                  │   Saves to: Assessments + Users tables
   └────┬─────────────┘
        │
        ↓
4. HOME DASHBOARD (PERSONALIZED)
   ┌─────────────────────────────────────────────────┐
   │            HOME DASHBOARD                       │
   ├─────────────────────────────────────────────────┤
   │  ┌─────────────────────────────────────────┐   │
   │  │  SUCCESS STORIES CAROUSEL               │   │
   │  │  - Matched by demographics + category   │   │
   │  │  - LLM ranks by relevance              │   │
   │  └─────────────────────────────────────────┘   │
   │                                                  │
   │  ┌─────────────────────────────────────────┐   │
   │  │  CAREER PATHWAY                         │   │
   │  │  - Step 1: Learn DAW Basics            │   │
   │  │  - Step 2: Build Portfolio             │   │
   │  │  - Step 3: Network & Mentors           │   │
   │  │  - Generated by LLM                    │   │
   │  └─────────────────────────────────────────┘   │
   │                                                  │
   │  ┌─────────────────────────────────────────┐   │
   │  │  MATCHED MENTORS                        │   │
   │  │  [Mentor 1] [Mentor 2] [Mentor 3]      │   │
   │  │  Schedule Meeting buttons              │   │
   │  └─────────────────────────────────────────┘   │
   │                                                  │
   │  ┌─────────────────────────────────────────┐   │
   │  │  RECOMMENDED COURSES                    │   │
   │  │  [Music Production 101] [Beat Making]  │   │
   │  │  Take Course buttons                   │   │
   │  └─────────────────────────────────────────┘   │
   └─────────────────────────────────────────────────┘
        │
        │ User clicks "Take Course"
        ↓
5. COURSE LEARNING
   ┌─────────────────────────────────────────┐
   │         COURSE PAGE                     │
   ├─────────────────────────────────────────┤
   │  Progress: [████████░░░░░░] 35%        │
   │                                         │
   │  Module 1: Introduction                │
   │    ✓ Lesson 1-1: Getting Started      │
   │    ✓ Lesson 1-2: Understanding MIDI   │
   │  Module 2: Beat Making                 │
   │    → Lesson 2-1: Drum Programming     │ ← Currently here
   │      Lesson 2-2: Melody Creation      │
   │                                         │
   │  [Mark as Complete] button             │
   └─────────────────────────────────────────┘
        │
        │ Progress updates in DynamoDB
        │ When reaches 25%...
        ↓
6. WELLNESS CHECK (Triggered at 25%)
   ┌─────────────────────────────────────────┐
   │      WELLNESS CHECK CHATBOT             │
   ├─────────────────────────────────────────┤
   │  AI: "How are you feeling about this   │
   │      course so far?"                    │
   │                                         │
   │  User: "It's great!"                   │
   │                                         │
   │  AI: "Is the career path still feeling │
   │      right for you?"                    │
   │                                         │
   │  LLM Analyzes → 3 Outcomes:            │
   │  1. HAPPY → Continue course            │
   │  2. UNHAPPY_WITH_COURSE →              │
   │     - Suggest mentor meeting           │
   │     - Show alternative subcategories   │
   │  3. UNHAPPY_WITH_CATEGORY →            │
   │     - Redirect to Career Advisor       │
   └─────────────────────────────────────────┘
        │
        │ If HAPPY, continue...
        │ Progress continues to 100%
        ↓
7. COURSE COMPLETION & CERTIFICATION
   ┌─────────────────────────────────────────┐
   │      CERTIFICATION PAGE                 │
   ├─────────────────────────────────────────┤
   │         🎉 CONGRATULATIONS! 🎉         │
   │                                         │
   │    Certificate of Completion           │
   │    Music Production Fundamentals       │
   │                                         │
   │    Awarded to: John Doe                │
   │    Date: 2024-01-15                    │
   │    Certificate #: SPARK-2024-ABC123    │
   │                                         │
   │    [Download PDF] [Share]              │
   │                                         │
   │    → Connect with Peers →              │
   └─────────────────────────────────────────┘
        │
        ↓
8. PEER NETWORKING
   ┌─────────────────────────────────────────┐
   │       NETWORKING PAGE                   │
   ├─────────────────────────────────────────┤
   │  People with similar courses:          │
   │                                         │
   │  ┌────────────┐  ┌────────────┐       │
   │  │ Sarah J.   │  │ Mike L.    │       │
   │  │ Producer   │  │ Producer   │       │
   │  │ Atlanta,GA │  │ LA, CA     │       │
   │  │ 2 common   │  │ 1 common   │       │
   │  │  courses   │  │  course    │       │
   │  │ [Connect]  │  │ [Connect]  │       │
   │  └────────────┘  └────────────┘       │
   └─────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    RETURNING USER JOURNEY                            │
└─────────────────────────────────────────────────────────────────────┘

1. LOGIN
   ┌──────────┐
   │  Login   │ → Email + Password
   │   Page   │   JWT Token generated
   └────┬─────┘
        │
        │ Check: isNewUser === false
        ↓
2. HOME DASHBOARD (Direct Access)
   → Immediately see personalized content
   → Resume courses where they left off
   → All features accessible
```

## Technical Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                    SYSTEM ARCHITECTURE                               │
└─────────────────────────────────────────────────────────────────────┘

CLIENT (React)
   │
   │ HTTP Requests
   ├──────────────────────► API ROUTES (Express)
   │                        │
   │                        ├─ /api/auth/* → JWT Authentication
   │                        ├─ /api/stories/* → Success Stories
   │                        ├─ /api/pathway/* → Career Pathways
   │                        ├─ /api/mentors/* → Mentor Matching
   │                        ├─ /api/courses/* → Course Management
   │                        ├─ /api/progress/* → Progress Tracking
   │                        ├─ /api/certifications/* → Certificates
   │                        └─ /api/networking/* → Peer Discovery
   │                        │
   │                        ↓
   │                   SERVICES LAYER
   │                        │
   │                        ├─► DynamoDB Service
   │                        │   - CRUD operations
   │                        │   - 9 tables with GSIs
   │                        │
   │                        └─► Bedrock Service
   │                            - LLM invocation
   │                            - Prompt engineering
   │                            - Response parsing
   │
   │ WebSocket (Socket.io)
   └──────────────────────► SOCKET HANDLER
                            │
                            ├─ start-assessment
                            ├─ user-message
                            ├─ start-wellness-check
                            └─ wellness-response
                            │
                            ↓
                       AWS BEDROCK (Claude)
                            │
                            ↓
                       AI Response
                            │
                            ↓
                       Emit to Client

DATA FLOW:
   User Input → Socket.io → Bedrock → LLM Response → Socket.io → User

PERSISTENCE:
   All data → DynamoDB Tables:
   - Users, Assessments, Courses, Progress, Mentors,
     Stories, ChatHistory, Certifications, Pathways
```

## Database Relationships

```
┌──────────────┐
│    USERS     │
│  (userId)    │
└──┬───────────┘
   │
   │ One-to-Many
   ├──────────────┬──────────────┬─────────────┬────────────┐
   ↓              ↓              ↓             ↓            ↓
┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ASSESSME│ │ PROGRESS │ │ PATHWAYS │ │ CERTIFIC │ │   CHAT   │
│  NTS   │ │          │ │          │ │  ATIONS  │ │ HISTORY  │
└─────────┘ └────┬─────┘ └──────────┘ └────┬─────┘ └──────────┘
                 │                          │
                 │ References              │ References
                 ↓                          ↓
           ┌──────────┐              ┌──────────┐
           │ COURSES  │              │ COURSES  │
           └──────────┘              └──────────┘

STANDALONE TABLES:
┌──────────┐  ┌──────────┐
│ MENTORS  │  │ SUCCESS  │
│          │  │ STORIES  │
└──────────┘  └──────────┘
(Matched to users via category/demographics, not foreign keys)
```

## API Request Examples

```
POST /api/auth/signup
Body: { email, password, firstName, lastName, race, ethnicity, location }
Response: { token, user }

POST /api/auth/login
Body: { email, password }
Response: { token, user }

GET /api/stories/matched
Headers: Authorization: Bearer {token}
Response: { stories: [...] } // Ranked by LLM

GET /api/pathway/:userId
Response: { pathway: { steps: [...] } }

GET /api/courses/recommended/:userId
Response: { courses: [...] }

PUT /api/progress/update
Body: { userId, courseId, lessonId }
Response: { progress, needsWellnessCheck: true/false }

POST /api/certifications/generate
Body: { userId, courseId }
Response: { certification: {...} }
```

## Socket.io Events

```
CLIENT → SERVER:
- start-assessment: { userId }
- user-message: { message }
- start-wellness-check: { userId, courseId }
- wellness-response: { message }

SERVER → CLIENT:
- assistant-message: { message, questionNumber? }
- assessment-complete: { category, subcategories, reasoning, assessmentId }
- wellness-complete: { outcome, reasoning, recommendation }
- error: { message }
```

## AWS Services Integration

```
APPLICATION
    │
    ├─► AWS DynamoDB
    │   - Tables: 9 total
    │   - Indexes: GSIs for queries
    │   - Provisioned: 5 RCU/WCU (free tier)
    │
    ├─► AWS Bedrock
    │   - Model: Claude 3 Sonnet
    │   - Use: Conversational AI
    │   - Pricing: Pay-per-token
    │
    └─► AWS EC2 (Deployment)
        - Instance: t2.micro (free tier)
        - OS: Amazon Linux 2
        - Services: Node.js + PM2 + Nginx
```

## Security Flow

```
1. User signs up → Password hashed (bcrypt)
2. User logs in → JWT token generated (7-day expiry)
3. Client stores token → localStorage
4. Every API request → Token in Authorization header
5. Middleware verifies → jwt.verify(token, secret)
6. If valid → req.user populated, proceed
7. If invalid → 403 error
```

---

This system delivers a complete, production-ready career discovery platform with AI at its core!
